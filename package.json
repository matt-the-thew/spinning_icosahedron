{
  "name": "rotating_icosahedron",
  "version": "1.0.0",
  "description": "Sets up a simple scene using the Three.js library, and then renders that scene in a `<canvas>` tag that is appended to the html document.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "vite": "^7.1.10"
  }
}
